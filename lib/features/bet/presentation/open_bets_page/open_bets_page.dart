import 'package:flutter/material.dart';
import 'package:sp8/core/constants/app_sizes.dart';

class OpenBetsPage extends StatelessWidget {
  const OpenBetsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        gapH8,
        Center(child: Text("Best Description")),
        gapH8,
        Center(child: Text("Best Description")),
        gapH8,
        Center(child: Text("Best Description")),
        gapH8,
        Center(child: Text("Best Description")),
        gapH8,
        Center(child: Text("Best Description")),
        gapH8,
        Center(child: Text("Best Description")),
        gapH8,
        Center(child: Text("Best Description")),
      ],
    );
  }
}
